{
  "name": "asad",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.6.1",
  "scripts": {
    "build": "pnpm -r build",
    "check": "pnpm -r check",
    "api:start": "node apps/api/dist/apps/api/src/server.js",
    "fixtures:generate": "node scripts/generate-fixtures.mjs",
    "pdf:sample": "node scripts/render-sample-pdf.mjs",
    "asset:probe": "node scripts/probe-local-asset.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
